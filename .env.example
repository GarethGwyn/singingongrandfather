# Stripe — get these from dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=sk_live_...
PUBLIC_STRIPE_KEY=pk_live_...

# Stripe Webhook — get this after creating the webhook endpoint in the dashboard
# dashboard.stripe.com → Developers → Webhooks → Add endpoint
# Endpoint URL: https://your-domain.vercel.app/api/stripe-webhook
STRIPE_WEBHOOK_SECRET=whsec_...
